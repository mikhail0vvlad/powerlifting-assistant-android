# Contributing

Branching: `develop` is the integration branch, features go into `feature/*`,
bug fixes into `fix/*`. PRs squash-merge into `develop`. `main` is release-only.

Commits follow Conventional Commits (`feat`, `fix`, `refactor`, `chore`, …).
